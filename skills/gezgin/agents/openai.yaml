interface:
  display_name: "Gezgin"
  short_description: "AI-first travel and city communities"
  default_prompt: "Use $gezgin to explore places, people, activities, events, and city communities, then coordinate only my chosen action."

dependencies:
  tools:
    - type: "mcp"
      value: "gezgin"
      description: "Source-backed Gezgin travel and community tools"
      transport: "streamable_http"
      url: "https://gezgin.com/mcp"

policy:
  allow_implicit_invocation: true
